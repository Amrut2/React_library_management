// DownloadPage.js
import React from 'react';

function DownloadPage({ subscriptionCardData }) {
  return (
    <div>
      {/* First Card */}
      <div className='card' style={{ boxShadow: '0 4px 8px rgba(0, 0, 0, 0.2)' }}>
        {/* ... Card content similar to the preview page ... */}
      </div>

      {/* Second Card */}
      <div className='card'>
        {/* ... Card content similar to the preview page ... */}
      </div>
    </div>
  );
}

export default DownloadPage;
